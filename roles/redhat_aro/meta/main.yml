---
dependencies:
  - role: common_user
  - role: dpkg_architecture
  - role: docker
  - role: githubcli
  - role: golang
  - role: usbip_client
    when: usbip_client | default(False)
  - role: usbip_server
    when: usbip_server | default(False)
